interface:
  display_name: "会议 PPT 子导演"
  short_description: "基于主内容池生成 PPT 方向引导 JSON"
  default_prompt: "Use $meeting-content-ppt-cn with the transcript and content-pool.json to generate ppt-content-package.json."
